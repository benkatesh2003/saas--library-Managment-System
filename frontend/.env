# Backend API URL (for when live integration is activated)
VITE_API_BASE_URL=http://localhost:5000/api

# Standalone mock mode (defaults to true so frontend is fully functional without running backend)
VITE_USE_MOCK=false

# Branding
VITE_APP_NAME=Library Sathi
